// this info type is primarily used for:
    // updating the filters
    // filtering the tasks
type FiltersInfo = {
    selectedDay: Date;
    title: string;
    status: string;
}

export default FiltersInfo;